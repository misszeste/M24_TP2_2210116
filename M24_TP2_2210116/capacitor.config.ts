import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'M24_TP2_2210116',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
